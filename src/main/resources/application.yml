spring:
  datasource:
    url: jdbc:user://localhost:3306/local
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: none

  mvc:
    throw-exception-if-no-handler-found: true
  resources:
    add-mappings: false
env:
  github-repository:
    entry-point: https://raw.githubusercontent.com
